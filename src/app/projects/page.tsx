import React from "react";
import Link from "next/link";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Link href="/">Projects Page</Link>
    </div>
  );
};

export default page;
